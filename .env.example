# Anthropic API key for the complaint-drafting endpoint (Phase 3)
ANTHROPIC_API_KEY=

# Override the SQLite database location (optional; defaults to ./data/app.db)
# APP_DB_PATH=

# Optional: NYC GeoSearch base URL override (defaults to public Pelias instance)
# GEOSEARCH_BASE_URL=https://geosearch.planninglabs.nyc/v2
