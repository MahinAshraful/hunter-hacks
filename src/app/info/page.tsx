import InfoStory from './story';

export const metadata = {
  title: 'Info — Am I Rent Stabilized?',
  description:
    'How a million New York apartments became invisible — and what we built to bring them back into view.',
};

export default function InfoPage() {
  return <InfoStory />;
}
